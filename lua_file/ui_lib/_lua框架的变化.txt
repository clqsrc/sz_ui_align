

lua 框架其实已经很稳定，目前的修改主要是以下几个原因：

引入了 tsc 自动生成后，对成员函数都自带了 self 参数，导致需要调用成员函数的接口都要修改（如果没有带 self 参数为第一个参数的话）

1.2022.09.18
--//2022 因为 tsc 给成员函数都生成了 self 参数，所以不得不修改，其他事件函数也一样要修改。不过其实也就是判断一下参数个娄就可以了
function UI_OnClick(s_key)

在 lua_functions_event.lua 中


