
每个语言环境下的 ndk_cpp 目录并不完全相同，这里尽量放相同的部分。原始版本是 android 的，所以名称带有了 ndk

--------------------------------------------------------
1.
java 版本的入口在

//因为权限的问题，所以实际上要得到权限后才做后面的事情
    protected void onCreate_AfterCheckPermission()

this.COnCreate(p2, GRoot_UiJson);

创建了一个 c 语言版本的主窗口。后面精简版本的话应该会去掉这部分，将生成 lua 运行库的方法提取出来。
并且应该要与 js 引擎做成可选的（目前是 2 选 1）

--------------------------------------------------------

